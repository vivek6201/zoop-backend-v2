import { Request, Response } from "express";

export const createAdminProfileController = async (
  req: Request,
  res: Response
) => {};

export const getAdminProfileController = async (
  req: Request,
  res: Response
) => {};

export const updateAdminProfileController = async (
  req: Request,
  res: Response
) => {};
